database
utente: nome, passwrd, bolean se è aministratore 
evento
to do list
attività (del to do list)
giorno : etichetta (giornata impegnata/fuori casa/relax/mestruazione/ecc...)

schema:
utente(username (PK), password, amministratore)
etichette(id (PK), titolo, sottotitolo, colore, username (FK))
etichetta_giorno(idEtichetta(PK, FK), dataGiorno (PK) )
evento(id (PK), titolo, descrizione, colore, orarioInizio, orarioFine, dataInizio, dataFine, username(FK) )
toDoList(id (PK), titolo, urgenza, giorno, username(FK))
attività(id (PK), titolo, descrizione, completata, idToDoList(FK) )

